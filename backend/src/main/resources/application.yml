spring:
  application:
    name: food-delivery
  datasource:
    url: jdbc:postgresql://localhost:5432/quickbite
    username: quickbite_user
    password: quickbite_pass123

  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
